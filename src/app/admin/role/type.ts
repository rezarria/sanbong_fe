export type EditModel = {
  id: string;
  name: string;
  displayName: string;
  lastModifiedDate: string;
};

export type ViewModel = {
  id: string;
  name?: string;
  displayName: string;
};

export type AddType = {
  name: string;
  displayName: string;
};

export type ListType = {
  id: string;
  name: string;
  displayName: string;
};
